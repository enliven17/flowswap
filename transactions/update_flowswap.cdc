import FlowSwap from 0x0c0c904844c9a720

transaction() {
  prepare(signer: auth(Storage, Capabilities) &Account) {
    // This transaction will update the FlowSwap contract
  }
  
  execute {
    // The contract update will be handled by Flow CLI
  }
} 